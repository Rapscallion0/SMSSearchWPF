<UserControl x:Class="SMS_Search.Views.Settings.ResultsSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ctrl="clr-namespace:SMS_Search.Views.Controls"
             xmlns:vm="clr-namespace:SMS_Search.ViewModels.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ResultsSectionViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel>
        <GroupBox Header="Grid Appearance">
            <StackPanel Margin="5">
                <ctrl:SettingControl Header="Show Row Numbers" IsSaved="{Binding ShowRowNumbers.IsSaved}">
                    <CheckBox IsChecked="{Binding ShowRowNumbers.Value}" Content="Display row numbers in search results" />
                </ctrl:SettingControl>

                <ctrl:SettingControl Header="Highlight Matches" IsSaved="{Binding HighlightMatches.IsSaved}">
                    <CheckBox IsChecked="{Binding HighlightMatches.Value}" Content="Highlight search terms in results" />
                </ctrl:SettingControl>

                <ctrl:SettingControl Header="Highlight Color" IsSaved="{Binding HighlightColor.IsSaved}">
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding PickHighlightColorCommand}" ToolTip="Pick Color" Margin="0,0,10,0" Padding="2">
                            <Border Width="40" Height="15" BorderBrush="Gray" BorderThickness="1" Background="{Binding HighlightColor.Value}" />
                        </Button>
                        <TextBlock Text="{Binding HighlightColor.Value}" VerticalAlignment="Center" />
                    </StackPanel>
                </ctrl:SettingControl>

                <ctrl:SettingControl Header="Resize Columns" IsSaved="{Binding ResizeColumns.IsSaved}">
                    <CheckBox IsChecked="{Binding ResizeColumns.Value}" Content="Automatically resize columns to fit content" />
                </ctrl:SettingControl>

                <ctrl:SettingControl Header="Auto-Resize Row Limit" IsSaved="{Binding AutoResizeLimit.IsSaved}">
                     <ctrl:ClearableTextBox Text="{Binding AutoResizeLimit.Value, UpdateSourceTrigger=PropertyChanged}" />
                </ctrl:SettingControl>

                <ctrl:SettingControl Header="Description Columns" IsSaved="{Binding DescriptionColumns.IsSaved}">
                    <CheckBox IsChecked="{Binding DescriptionColumns.Value}" Content="Show description columns for codes" />
                </ctrl:SettingControl>

                <ctrl:SettingControl Header="Horizontal Scroll Speed" IsSaved="{Binding HorizontalScrollSpeed.IsSaved}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Slow" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock DockPanel.Dock="Right" Text="Fast" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        <Slider Value="{Binding HorizontalScrollSpeed.Value}" Minimum="4" Maximum="48" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" />
                    </DockPanel>
                </ctrl:SettingControl>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>
