<UserControl x:Class="SMS_Search.Views.Settings.Gs1ToolkitSettingsSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SMS_Search.ViewModels.Settings"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance vm:Gs1ToolkitSettingsSectionViewModel}">
    <Grid>
        <StackPanel>
            <TextBlock Text="Clipboard Integration" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#333333"/>

            <CheckBox IsChecked="{Binding MonitorClipboard.Value, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Monitor clipboard for GS1 barcodes" FontWeight="SemiBold" />
                    <TextBlock Text="Automatically prompts to open toolkit if a GS1 string is detected." Foreground="Gray" FontSize="12" TextWrapping="Wrap" />
                </StackPanel>
            </CheckBox>
        </StackPanel>
    </Grid>
</UserControl>