<UserControl x:Class="SMS_Search.Views.Settings.CleanSqlSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ctrl="clr-namespace:SMS_Search.Views.Controls"
             xmlns:vm="clr-namespace:SMS_Search.ViewModels.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CleanSqlSectionViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel Margin="0,0,0,20">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
             <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
             <StackPanel Orientation="Horizontal" Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="10,0,0,0">
                  <TextBlock Text="Saving..." Foreground="Blue" VerticalAlignment="Center" />
             </StackPanel>
             <StackPanel Orientation="Horizontal" Visibility="{Binding IsSaved, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="10,0,0,0">
                  <Path Data="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" Fill="Green" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" />
                  <TextBlock Text="Saved" Foreground="Green" Margin="5,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" />
             </StackPanel>
        </StackPanel>

        <DockPanel LastChildFill="True">
             <StackPanel DockPanel.Dock="Right" Margin="10,0,0,0">
                  <Button Content="Add Rule" Command="{Binding AddRuleCommand}" Margin="0,0,0,5" Width="100"/>
                  <Button Content="Remove Rule" Command="{Binding RemoveRuleCommand}" Margin="0,0,0,5" Width="100"/>
                  <Button Content="Restore Defaults" Command="{Binding RestoreDefaultsCommand}" Width="100"/>
             </StackPanel>

             <DataGrid ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
                  <DataGrid.Columns>
                       <DataGridTextColumn Header="Regex Pattern" Binding="{Binding Pattern, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                       <DataGridTextColumn Header="Replacement" Binding="{Binding Replacement, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                  </DataGrid.Columns>
             </DataGrid>
        </DockPanel>
    </StackPanel>
</UserControl>
