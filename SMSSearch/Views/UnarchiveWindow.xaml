<Window x:Class="SMS_Search.Views.UnarchiveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SMS_Search.ViewModels"
        mc:Ignorable="d"
        Title="Unarchive" Height="130" Width="130"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        AllowDrop="True" Drop="Window_Drop"
        MouseDown="Window_MouseDown"
        Closing="Window_Closing">
    <Grid Background="Transparent">
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Close target" Command="{Binding CloseCommand}"/>
            </ContextMenu>
        </Grid.ContextMenu>
        <Control Template="{StaticResource Icon_UnarchiveNew}" ToolTip="Drop files here to unarchive" />
    </Grid>
</Window>
