<Window x:Class="SMS_Search.Views.Windows.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SMS Search Update" Height="400" Width="500"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="White" FontFamily="Segoe UI">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="An update is available!" FontWeight="SemiBold" FontSize="18" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15" Background="#F9F9F9" Padding="10">
            <TextBlock x:Name="ChangelogText" TextWrapping="Wrap" />
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel x:Name="ActionPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Yes" Width="80" Height="30" Margin="0,0,10,0" Click="YesButton_Click"/>
            <Button Content="No" Width="80" Height="30" Click="NoButton_Click"/>
        </StackPanel>

        <!-- Progress Reporting (Initially Hidden) -->
        <StackPanel x:Name="ProgressPanel" Grid.Row="3" Visibility="Collapsed">
            <TextBlock x:Name="StatusTextBlock" Text="Preparing..." Margin="0,0,0,5" FontSize="13"/>
            <ProgressBar x:Name="UpdateProgressBar" Height="15" Minimum="0" Maximum="100"/>
        </StackPanel>
    </Grid>
</Window>
