<Window x:Class="SMS_Search.Views.ModernSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SMS_Search.ViewModels.Settings"
        xmlns:views="clr-namespace:SMS_Search.Views.Settings"
        mc:Ignorable="d"
        Title="Settings" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:GeneralSectionViewModel}">
            <views:GeneralSectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ConnectionSectionViewModel}">
            <views:ConnectionSectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DisplaySectionViewModel}">
            <views:DisplaySectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CleanSqlSectionViewModel}">
            <views:CleanSqlSectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LauncherSectionViewModel}">
            <views:LauncherSectionView />
        </DataTemplate>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" BorderThickness="0" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border BorderBrush="#DDDDDD" BorderThickness="0,0,1,0" Background="#F9F9F9">
            <ListBox x:Name="Sidebar" ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}"
                     BorderThickness="0" Background="Transparent" SelectionChanged="Sidebar_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <!-- Icon -->
                            <Path Data="{Binding IconData}" Fill="#555555" Width="16" Height="16" Stretch="Uniform" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Main Content -->
        <ScrollViewer x:Name="MainScrollViewer" Grid.Column="1" Padding="20" ScrollChanged="MainScrollViewer_ScrollChanged">
            <ItemsControl x:Name="SectionItemsControl" ItemsSource="{Binding Sections}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                         <Border Margin="0,0,0,40"> <!-- Spacing between sections -->
                             <StackPanel>
                                 <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Margin="0,0,0,5" />
                                 <Separator Margin="0,0,0,15" />
                                 <ContentPresenter Content="{Binding}" />
                             </StackPanel>
                         </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
