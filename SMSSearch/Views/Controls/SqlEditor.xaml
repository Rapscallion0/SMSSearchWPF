<UserControl x:Class="SMS_Search.Views.Controls.SqlEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Border BorderBrush="Gray" BorderThickness="1">
            <avalon:TextEditor x:Name="Editor"
                               ShowLineNumbers="True"
                               FontFamily="{Binding EditorFontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontSize="{Binding EditorFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               WordWrap="True"
                               VerticalScrollBarVisibility="Auto"
                               HorizontalScrollBarVisibility="Auto"
                               TextChanged="Editor_TextChanged">
                <avalon:TextEditor.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="ApplicationCommands.Undo" Header="Undo"/>
                        <MenuItem Command="ApplicationCommands.Redo" Header="Redo"/>
                        <Separator/>
                        <MenuItem Command="ApplicationCommands.Cut" Header="Cut"/>
                        <MenuItem Command="ApplicationCommands.Copy" Header="Copy"/>
                        <MenuItem Command="ApplicationCommands.Paste" Header="Paste"/>
                        <MenuItem Command="ApplicationCommands.Delete" Header="Delete"/>
                        <Separator/>
                        <MenuItem Command="ApplicationCommands.SelectAll" Header="Select All"/>
                        <MenuItem Header="Clear All" Click="ClearAll_Click"/>
                    </ContextMenu>
                </avalon:TextEditor.ContextMenu>
            </avalon:TextEditor>
        </Border>
    </Grid>
</UserControl>
